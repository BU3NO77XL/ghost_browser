# cookies.txt (Netscape HTTP Cookie File)
#
# Use ONLY with sites/accounts you own or have explicit authorization to test.
# This file is read automatically by navigate(..., inject_cookies=True) when
# cookies_file="cookies.txt" (default).
#
# Format (TAB-separated, 7 columns):
#   domain  include_subdomains  path  secure  expires_epoch  name  value
#
# Notes:
# - Comment lines start with '#'
# - HttpOnly cookies can be prefixed with '#HttpOnly_' on the domain field:
#     #HttpOnly_.example.com   TRUE  /  TRUE  0  sessionid  abc123
# - expires_epoch:
#     0 = session cookie (no fixed expiry)
#     otherwise Unix epoch seconds
#
# Example (replace with your target site's cookie(s)):
# .example.com	TRUE	/	TRUE	0	sessionid	PASTE_YOUR_SESSION_COOKIE_VALUE_HERE
#
# Add your site's auth/session cookies below (TAB-separated):

